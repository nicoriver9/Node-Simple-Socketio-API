{
  "name": "telemetria",
  "version": "1.0.0",
  "description": "* run npm install\r * run dev on context with run npm dev\r * buid with tsc",
  "main": "index.js",
  "scripts": {
    "start" : "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-handlebars": "^7.0.1",
    "socket.io": "^4.6.1"
  }
}
